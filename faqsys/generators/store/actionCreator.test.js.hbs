import { {{camelCase actionName}} } from '../actionCreator'
import { {{snakeCase actionName}} } from '../actionType'

describe('{{ properCase name }} actions', () => {
  describe('#{{camelCase actionName}}', () => {
    it('has a type of {{snakeCase actionName}}', () => {
      const expected = {
        type: {{snakeCase actionName}},
      }
      expect({{camelCase actionName}}()).toEqual(expected)
    })
  })
})
