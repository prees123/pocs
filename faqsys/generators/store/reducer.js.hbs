import { {{snakeCase actionName}} } from './actionType'

const initialState = {}

export default function {{camelCase name}}Reducer (state = initialState, action) {
  switch (action.type) {
    case {{snakeCase actionName}}:
      return Object.assign({}, state, {
      })
    default:
      return state
  }
}
