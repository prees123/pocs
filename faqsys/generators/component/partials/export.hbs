export default {{space}}

{{~#if isConnected}}
 connect(mapStateToProps, mapDispatchToProps)(
{{~/if~}}

{{~#if isStyled}}
  withStyles(styles)(
{{~/if~}}

{{ space }}{{ properCase name }}

{{~#if isStyled~}}
  )
{{~/if~}}

{{~#if isConnected~}}
  )
{{~/if~}}
