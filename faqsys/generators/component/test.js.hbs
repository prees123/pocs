import React from 'react'
import { shallow } from 'enzyme'

import {{ properCase name }} from '../{{ properCase name }}'

describe('<{{ properCase name }} /> component', () => {
  it('renders', () => {
    const {{ camelCase name }} = shallow(
      <{{ properCase name }} />
    )
    expect({{ camelCase name }}).toHaveLength(1)
  })
})
